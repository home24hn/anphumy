/** Shared visual language for the 5 illustration scenes — same stroke
 * weights and palette so they read as one consistent system. */
export const INK = "#12243A"; // structure / primary line work
export const MUTED = "#94AFCB"; // secondary / recessive detail
export const ACCENT = "#2563EB"; // active technology — connections, nodes

export const STROKE_MAIN = 1.75;
export const STROKE_SECONDARY = 1.25;
export const STROKE_ACCENT = 1.5;
